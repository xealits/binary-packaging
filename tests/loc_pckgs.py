"""Bunch of functions implementing packaging 1 scheme."""


def store_packages(pckg_names: list, sources: list, store_path: str):
    """store_packages(pckg_names: list(str<pckg_name>),
             sources: list(str<dir_path>),
             store_path: str<store_path>))

    Not implemented. Done in Fish shell.
    """

    pass
